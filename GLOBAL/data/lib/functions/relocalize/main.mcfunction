# Executed globally
# tellraw @a [{"text":"GLOBAL"},{"text":" > lib:relocalize/main","color":"gray"}]

# If the function is executed globally
execute as @a[scores={reloc.timer=1..}] at @s run function lib:relocalize/animation